# ===========================================
# Paperless-ngx - Variables de Entorno
# ===========================================
# Copia este archivo a .env y ajusta los valores
# Guía: https://www.eldiarioia.es/paperless-ngx-digitaliza-documentos-ia-homelab/

# --- Zona horaria ---
TZ=Europe/Madrid

# --- Permisos (tu usuario local) ---
# Obtén con: echo $UID && id -g
PUID=1000
PGID=1000

# --- Base de datos ---
PAPERLESS_DBNAME=paperless
PAPERLESS_DBUSER=paperless
PAPERLESS_DBPASS=cambia_esta_password_segura

# --- Administrador inicial ---
PAPERLESS_ADMIN_USER=admin
PAPERLESS_ADMIN_PASSWORD=cambia_esta_password_tambien

# --- OCR ---
# Idiomas: spa (español), eng (inglés), deu (alemán), fra (francés)
# Combina con +: spa+eng
PAPERLESS_OCR_LANGUAGE=spa+eng

# --- URL externa ---
# Cambia si usas reverse proxy o dominio
PAPERLESS_URL=http://localhost:8000

# --- Traefik (si usas docker-compose.traefik.yml) ---
PAPERLESS_DOMAIN=docs.example.com

# --- Tika/Gotenberg (documentos Office) ---
# Descomenta si necesitas procesar DOCX, XLSX, PPTX
# PAPERLESS_TIKA_ENABLED=true
